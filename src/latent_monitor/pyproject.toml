[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "latent-monitor"
version = "0.1.0"
description = "Controlled-variable latent-space monitoring: projectors, per-event displacement statistics, attribution lookup, adjustments (docs/EXPERIMENT_DESIGN.md Part III)"
requires-python = ">=3.10"
dependencies = ["numpy>=1.24", "scipy>=1.10", "noise-module"]

[project.optional-dependencies]
torch = ["torch>=2.0"]

[tool.setuptools]
packages = ["latent_monitor", "latent_monitor.estimators"]

[tool.setuptools.package-dir]
latent_monitor = "."
"latent_monitor.estimators" = "estimators"

[tool.uv.sources]
noise-module = { workspace = true }
