# §A pre-implementation review — 31 August 2026

_Run by an independent reviewing agent against `REVIEW_PROMPTS.md` §A, before
any code under `src/oracle_diag/` or `experiments/` existed. Inputs:
`EXPERIMENT_DESIGN.md`, `IMPLEMENTATION_PLAN.md` (rev. 1), the archive docs,
`paper3_proposal.tex`. `PREREGISTRATION.md` did not exist (finding B1); WP0 was
used as the stand-in. Disposition column added by the authors; every blocker
is resolved in `IMPLEMENTATION_PLAN.md` rev. 2, marked `[A-Bn]`._

## Blockers

| # | Finding (abridged; quoted text in the original report) | Disposition |
|---|---|---|
| B1 | `PREREGISTRATION.md` does not exist; four gates depend on it | WP0 now specifies three hashed parts (`core`/`counts`/`bridge`); `core` is written and frozen at M0 |
| B2 | The paper's own statistic — Σ⁻¹-whitened displacement *pulled back through the Jacobian* — was not in WP2; two separate metrics were | `PullbackWhitenedDisplacement(Σ)` = ‖Σ^{-1/2} J δZ‖ added with analytic AC; standalone Σ⁻¹-at-representation dropped as ill-defined |
| B3 | cov_B's Jacobian arm is true by construction (perturbations and monitor share the Jacobian); the whitened arm had no derivation | `Metric.is_analytic_for(family)` marks by-construction cells; cov_B's falsifiable content restated as (i) K ≈ 0 on null vs large on aligned, (ii) empirical failure of magnitude-only baselines, (iii) `random` in between; whitened arm now *is* the pullback metric |
| B4 | N-vs-S contrast still not content-matched (S families "assigned", not matched) — audit P1.1 unfixed | Primary C2 endpoint = N vs content-matched clean twins; S families secondary and additionally matched to their assigned N cell |
| B5 | Matching under-specified (caliper, replacement, unmatched disposition) despite a ≥ 90 % AC | Caliper 1.0 standardized Euclidean; 1:1 without replacement; unmatched N retained in a declared `N*_unmatched` cell and reported; match rate reported; pilot measures it before production |
| B6 | Freeze ordering self-contradictory (M2 "citable" before M3 "frozen"); re-freezing had no semantics | Versioned freeze parts; cov_B depends on `core` only; results carry the hashes of the parts they depend on |
| B7 | WP3's AC measured cov_B's K result on the real model before the prediction was registered | WP3 AC restricted to linear/synthetic models; real-model K is cov_B's confirmatory result only |
| B8 | Two claim ladders (C3 = cost vs C3 = abstention); C5 had no WP; E0 dropped; E1–E4/T1 had no endpoints | Proposal's ladder adopted everywhere; abstention folded into C2; cov_E added for C5 (patching only); E0 restored; endpoints for E0–E5 and T1 specified in WP0/WP9 |
| B9 | Equivalence margins for one comparison only; cov_A's prediction unfalsifiable | Margins for every comparison; cov_A: "degrades" = Δρ ≤ −0.2 with CI excluding 0; "does not" = |Δρ| ≤ 0.10 with CI inside |
| B10 | Prediction table had no decision rule | Columns (monitor, family, severity, stratum, endpoint, threshold, direction); analytic rows excluded; one summary statistic with binomial CI; no add/remove/reword after freeze |
| B11 | No multiplicity control | One primary endpoint per claim; BH-FDR 0.05 over secondaries within claim; §5 scored as one statistic |
| B12 | Metric promotion "adds separation beyond the generic arm" = selection on the baseline comparison | Promotion on positive-control monotonicity only; baselines get a declared, equal tuning grid |

## Should fix — all adopted

Model seed added as a bootstrap level · D5 now gates the Tier-2 *confirmatory*
run (angular error is K) · injection cylinder sized against ARCA · production
count is an output of cov_D · cov_D null seeds disjoint from calibration seeds
· figure-provenance sidecar guard · "smoke check" cell removed from the claims
table · proposal Phase 2 changed from SPINE to Panda per D3 · schedule
restated as 4–6 calendar months with a cut order.

## Unsupported — dispositions

"Licence-clean" made conditional and recorded (§7.5) · "deployability" claimed
narrowly (§7.4) · the "1.5–3k lines / 80 % wiring" estimate is retained as an
estimate only in `EXPERIMENT_DESIGN.md` and does not appear in the plan ·
match-rate AC now measured by the 1k pilot · "referee-proof" softened to
"the strongest form these claims can take".

## Passed

Leakage (Metric.fit, U calibration) · FAR parity · pooled alarm–consequence
correlation refused by code · patching before repair · Σ identifiability ·
medium vs layout · toy-import guard with failure behaviour · testbed count ·
no use of "novel".
